allOf: 
  - $ref: Core.yml
  - type: object
    properties:
      '@type':
        type: string
        enum:
          - 'Event'
          - 'EventSeries'
          - 'ExhibitionEvent'
      kind:
        type: string
        enum:
          - 'culture'
      origin:
        type: string
      title: 
        $ref: Title.yml
      subTitle:
        $ref: Title.yml
      description:
        $ref: Description.yml
      shortDescription:
        $ref: Description.yml
      startDate:
        type: string
        format: date-time
      endDate:
        type: string
        format: date-time
      previousStartDate:
        type: string
        format: date-time
      doorTime:
        type: string
        format: date-time
      typicalAgeRange:
        type: string
      categories:
        type: array
        items:
          type: string
      keywords:
        type: array
        items:
          type: string
      inLanguages:
        type: array
        items:
          type: string
      isAccessibleForFree:
        type: boolean
      sameAs: 
        type: string
        format: uri
      visibility:
        type: string
        enum:
          - published
          - unpublished
          - draft
          - archived
          - restricted
      eventStatus:
        type: string
        enum:
          - cancelled
          - postponed
          - rescheduled
          - scheduled
      eventAttendanceMode:
        type: string
        enum:
          - offline
          - online
          - mixed
      location:
        $ref: Location.yml
      organizer:
        $ref: Organization.yml
      subEvents:
        type: array
        items:
          $ref: '#'
      superEvent:
        type: string
        format: identifier