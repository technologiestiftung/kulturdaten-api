allOf: 
  - $ref: Core.yml
  - type: object
    properties:
      '@type':
        type: string
        enum:
          - 'Organization'
      name:
        $ref: Title.yml
      description:
        $ref: Text.yml
      address:
        $ref: PostalAddress.yml
      contactPoint:
        type: array
        items:
          $ref: ContactPoint.yml
      website:
        type: string
      email:
        type: string
      telephone:
        type: string
      categories:
        type: array
        items:
          $ref: DefinedTerm.yml
    
