properties:
  success:
    type: boolean
    enum:
      - true
  message:
    type: string
  data:
    type: object
    properties:
      tag:
        $ref: "Tag.yml"
    additionalProperties: false
required:
  - success
additionalProperties: false
