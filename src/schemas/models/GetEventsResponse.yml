type: object
properties:
  success:
    type: boolean
  message:
    type: string
  data:
    type: object
    properties:
      page:
        type: number
      pageSize:
        type: number
      totalCount:
        type: number
      events:
        type: array
        items:
          $ref: "Event.yml"
      eventsReferences:
        type: array
        items:
          $ref: "Reference.yml"
required:
  - success
