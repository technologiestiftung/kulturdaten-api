type: object
properties:
  type: 
    type: string
    enum:
      - type.command.Organization.create
  payload:
    type: object
    properties:
      profile:
        $ref: 'OrganizationProfile.yml'
      status:
        $ref: 'OrganizationStatus.yml'
      address:
        $ref: 'Address.yml'
      borough:
        $ref: 'Borough.yml'
      classifications:
        $ref: 'OrganizationClassification.yml'
      contact:
        $ref: 'Contact.yml'