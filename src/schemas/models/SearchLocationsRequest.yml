type: object
properties:
  searchTerm:
    type: object
    properties:
      value:
        type: string
      matchType:
        type: string
        enum:
          - exact
          - contains
          - similar