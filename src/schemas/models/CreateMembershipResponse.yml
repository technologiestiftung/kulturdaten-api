properties:
  success:
    type: boolean
    enum:
      - true
  message:
    type: string
  data:
    type: object
    properties:
      email:
        type: string
      firstName:
        type: string
      lastName:
        type: string
      role:
        $ref: "Role.yml"
    additionalProperties: false
required:
  - success
additionalProperties: false
