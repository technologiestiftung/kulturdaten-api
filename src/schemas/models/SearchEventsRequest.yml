type: object
properties:
  matchMode:
    type: string
    enum: [any, all]
  searchFilter:
    type: object
    additionalProperties: true
  findEventsByAttractionTag:
    type: object
    properties:
      tags:
        type: array
        items:
          type: string
      matchMode:
        type: string
        enum: [any, all]