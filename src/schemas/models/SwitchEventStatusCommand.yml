type: object
properties:
  type: 
    type: string
    enum:
      - type.command.Event.switchStatus
  payload:
    type: object
    properties:
      eventStatus:
        type: string
        enum:
          - eventStatus.cancelled
          - eventStatus.postponed
          - eventStatus.rescheduled
          - eventStatus.scheduled