type: object
properties:
  type:
    type: string
    enum:
      - type.Tag
  identifier:
    type: string
  title:
    $ref: "TranslatableField.yml"
  metadata:
    allOf:
      - $ref: "Metadata.yml"
      - type: object
        properties:
          externalIDs:
            type: object
            additionalProperties:
              type: string
required:
  - identifier
  - title
